{
  "name": "scorm-api-wrapper",
  "version": "1.0.0",
  "description": "SCORM API Wrapper",
  "main": "scorm-api-wrapper.js",
  "scripts": {
    "build": "tsc",
    "test": "jasmine --reporter=jasmine-console-reporter --config=jasmine.json",
    "test:watch": "nodemon --ext ts --exec \"jasmine --reporter=jasmine-console-reporter --config=jasmine.json\""
  },
  "author": "szenadam",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^3.5.10",
    "jasmine": "^3.5.0",
    "jasmine-console-reporter": "^3.1.0",
    "nodemon": "^2.0.3",
    "ts-node": "^8.9.1",
    "typescript": "^3.8.3"
  }
}
